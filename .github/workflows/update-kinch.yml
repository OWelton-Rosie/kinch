name: Update Kinch Rankings

on:
  # Runs every 6 hours
  schedule:
    - cron: "0 0 * * 0"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  update:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Install dependencies
        run: npm install axios cheerio

      - name: Run scraper
        run: node scrape.js

      - name: Commit and push output.html
        run: |
          git config user.name "GitHub Actions"
          git config user.email "actions@github.com"
          git add output.html
          git diff --cached --quiet && echo "No changes to commit" || (git commit -m "Update Kinch Rankings" && git push)
